export function initialize(/* application */) {
  // TODO: This is just to get it to run with the old fastboot version,
  // as that otherwise breaks
  // This should be removed after updating fastboot to the latest version
}

export default {
  initialize
};
